# app_config.yml
# see 

application:
    COMPANY_NAME: Taylored Web Sites
    APP_NAME: Startup Authentication and Authorization app
    
    # Roles used for cancan implementation (see app/models/roles.rb, app/models/ability.rb)
    # each role has layout: (Application/All)(Role)s
    # ??? First role must be default guest role ???
    VALID_APP_ROLES: all_guest all_admin app1_user app1_admin
    DEFAULT_APP_ROLE: all_guest
    
development:
    SEND_EMAILS: true


production:
    SEND_EMAILS: true


test:
    SEND_EMAILS: false

