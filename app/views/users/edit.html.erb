<%
  @page_scope = 'users.edit'
  #page_title I18n.translate('title', :scope => @page_scope)
%>

<h4><%= I18n.translate('header', :scope => @page_scope) %></h4>

<%= form_for [@user], :action => 'update', do |f| %>

	<%= render :partial => 'layouts/errors_form', :locals => { :f => f } %>

	<div class="fields_20_80">
		<%= render :partial => 'form', :locals => { :f => f } %>
	</div>

	<div class="actions">
		<%= f.submit I18n.translate('view_action.update') %>
	</div>
	
<% end %>

<%= link_to I18n.translate('users.show.title'), user_path(@user.id) %>
 | <%= link_to I18n.translate('users.index.title'), users_path %>
 | <%= link_to I18n.translate('users.new.title'), new_user_path %>
